Replace ``indices=None`` with ``indices='all'`` as an alias for all-to-all connectivity in connectivity containers and all connectivity-computing functions with an ``indices`` parameter, by `Thomas Binns`_.