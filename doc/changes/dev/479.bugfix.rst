Fix frequency band averaging in :func:`~mne_connectivity.spectral_connectivity_time` with ``faverage=True``, which averaged over roughly twice the requested band when a band did not end at the top of the frequency grid, by `Marieke De Vylder`_.
