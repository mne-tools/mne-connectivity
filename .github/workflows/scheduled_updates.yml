name: Scheduled updates

on:  # yamllint disable-line rule:truthy
  schedule:
    - cron: '0 0 * * 1'  # At 00:00 every Monday
  pull_request:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'Enable ssh debugging'
        required: false
        default: false
        type: boolean

jobs:
  update:
    permissions:
      contents: write
      pull-requests: write
    name: ${{ matrix.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: spec_zero
            name: Update dependency specifiers
            title: 'MAINT: Update dependency specifiers'
            body: '*Adjustments may need to be made to shims in the codebase.*'
    env:
      GH_TOKEN: ${{ secrets.MNE_BOT_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.MNE_BOT_TOKEN }}
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: true
      - name: Get commit message
        run: echo "COMMIT_MESSAGE=$(git show -s --format=%s ${{ github.sha }})" | tee -a ${GITHUB_ENV}
      - name: Triage SSH
        run: |
          if [[ "${{ inputs.ssh }}" == "true" ]] || [[ "$COMMIT_MESSAGE" == *"[actions ssh]"* ]]; then
            echo "ENABLE_SSH=true" | tee -a $GITHUB_ENV
          else
            echo "ENABLE_SSH=false" | tee -a $GITHUB_ENV
          fi
      - name: Setup Remote SSH Connection
        if: env.ENABLE_SSH == 'true'
        uses: Warpbuilds/action-debugger@v1.3
        timeout-minutes: 10
        with:
          detached: true
      - name: Update tracked dependencies
        if: matrix.id == 'spec_zero'
        uses: mne-tools/mne-tools/actions/spec-zero-update-versions@main
        with:
          project-root: ${{ github.workspace }}
          changelog-path: 'doc/changes/dev/dependency.rst'
      - name: check if files changed
        run: |
          git diff && git status --porcelain
          if [[ $(git status --porcelain) ]]; then
            echo "dirty=true" >> $GITHUB_OUTPUT
          fi
        id: status
      - name: Create PR
        env:
          BRANCH: ${{ matrix.id }}
          TITLE: ${{ matrix.title }}
          BODY: ${{ matrix.body }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set -xeo pipefail
          git diff
          git config --global user.email "50266005+mne-bot@users.noreply.github.com"
          git config --global user.name "mne[bot]"
          git checkout -b "$BRANCH"
          git add -A  # doc/changes/dev/dependency.rst is a new file, the others are changed
          git commit -m "mne[bot]: ${TITLE#MAINT: }"
          git push origin "$BRANCH"
          printf '%s\n\n%s\n' \
            "Created by the \`$BRANCH\` job of this [GitHub Action run]($RUN_URL)." \
            "$BODY" > "$RUNNER_TEMP/pr_body.md"
          PR_NUM=$(gh pr create --base main --head "$BRANCH" --title "$TITLE" --body-file "$RUNNER_TEMP/pr_body.md")
          echo "Opened https://github.com/mne-tools/mne-connectivity/pull/${PR_NUM}" >> $GITHUB_STEP_SUMMARY
        if: steps.status.outputs.dirty == 'true' && false
